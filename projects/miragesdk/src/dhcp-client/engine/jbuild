(jbuild_version 1)

(executables
 ((names (main))
  (libraries (sdk lwt charrua-client.mirage charrua-client
              cmdliner fmt.cli logs.fmt logs.cli fmt.tty))
 ; (flags (:standard -cclib -static))
 ))
